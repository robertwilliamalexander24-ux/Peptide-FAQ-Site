# Peptide-FAQ

Source for the Peptide FAQ page.

- `index.html` — the FAQ content.

## Local preview

Open `index.html` directly in a browser, or serve the folder:

```
python3 -m http.server 8000
```

Then visit http://localhost:8000.
